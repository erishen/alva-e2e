# Part 1 补充：登录链路探索式测试策略与脚手架

> Part 2 的自动化套件只覆盖了**已发布的公开 Playbook（无人登录态）**的数据正确性。
> 笔试 Part 1 的旅程——**注册 → 建 Portfolio Watch Automation → 建 Playbook → 收 Alert**——
> 全部在登录态下，且涉及真实账号、反爬与限流，不适合纳入常态化巡检。
> 本文件给出**探索式测试的执行策略 + 问题报告模板 + 一套可运行的 Playwright 脚手架**，
> 你用自己注册的账号填好 `.env` 即可跑。

---

## 1. 时间盒策略（2 小时，按 JD 的「功能 × AI 各一半」切分）

| 时段 | 聚焦 | 产出 |
|---|---|---|
| 0–30 min | **功能正确性**：把 4 步旅程完整走通一遍，记录每一步的「预期 vs 实际」 | 关键路径冒烟结论 |
| 30–60 min | **功能边界 / 异常**：无效邮箱、重复注册、空组合、删除 Automation 后 Alert 是否还在、登出后 Playbook 权限 | 边界 bug 列表 |
| 60–90 min | **AI 功能正确性**（JD 重点）：让 Alva Agent 基于 Playbook 生成洞察/回答，核对它**引用的数据是否和页面展示一致**、有没有幻觉数字 | AI 一致性结论 |
| 90–120 min | **回归与整理**：复跑一遍最关键的 2–3 个场景，按严重度排序写报告 | 交付的问题报告 |

## 2. 优先级（按「用户影响 × 发生概率」）

1. **P0**：注册后无法登录 / 创建的 Automation 不触发任何 Alert / Playbook 生成失败。
2. **P1**：Alert  content 与 Playbook 数据对不上（如价格、涨跌幅错误）/ AI 回答引用了不存在的指标。
3. **P2**：UI 文案错误、空状态提示缺失、列表排序异常、移动端布局错位。
4. **P3**：文案错别字、图标错位、非关键动画卡顿。

## 3. 问题报告模板（每条都要有）

```
【严重度】P0 / P1 / P2 / P3
【模块】注册 / Automation / Playbook / Alert / AI Agent
【标题】一句话
【预期】…
【实际】…
【复现步骤】1. … 2. … 3. …
【证据】截图 / 录屏 / 接口响应（curl 或 Network 面板导出）
【是否偶发】必现 / 偶发（附出现频率）
```

## 4. 运行脚手架

脚手架在 `part1/onboarding.spec.ts`，**默认跳过**（无凭据时不跑，避免污染 Part 2 的绿集）。

```bash
cp part1/.env.example part1/.env   # 填入你注册的账号
# 然后：
npx playwright test part1/onboarding.spec.ts --project=chromium
```

脚手架覆盖了 4 步旅程的**关键路径 + 几处 AI 一致性校验**（如 Alert 里的价格应与 Playbook 行情 KPI 一致）。
注意：Alva 的表单类名/流程可能变动，脚手架里的选择器是**占位骨架**，需你跑起来后按真实 DOM 微调
（和 Part 2 一样，先 `node` 探测脚本确认真实结构再写断言，不要凭截图猜）。

## 5. 这套脚手架与 Part 2 套件的关系

- Part 2（本仓库 `tests/`）是**生产环境数据巡检**：公开、可无人值守、能抓 `$0.0` 这类真 bug。
- Part 1 脚手架是**登录态功能/AI 正确性**：需账号、适合人工触发或 PR 门禁，不进常态化巡检。
- 两者合起来才覆盖 JD 说的「功能正确性 50% + AI 功能正确性 50%」。
