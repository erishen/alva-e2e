# Part 1 脚手架不需要密码（你用 Gmail / Google SSO 登录）。
# 流程：
#   1) node part1/export-state.mjs   —— 浏览器手动用 Gmail 登录，自动导出会话到 part1/.auth/alva.json
#   2) npx playwright test part1/onboarding.spec.ts   —— 复用登录态，跳过账号填写
# part1/.auth/alva.json 已被 .gitignore 忽略，切勿提交。

WATCH_TICKER=NVDA

# 可选：被测站点基址（默认 https://alva.ai）
# BASE_URL=https://alva.ai
